# Lavu PR review skill

Use `lavu review <pr-number-or-url>` to inspect the full GitHub diff. Run six separate passes, produce actionable findings with exact file and line references, save the report, and place proposed comments in the approval queue. Use `lavu approve approve <id>` followed by `lavu approve send <id>` only after Dad has reviewed the staged output.
